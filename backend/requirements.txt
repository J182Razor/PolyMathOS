fastapi
uvicorn
# PyTorch - compatible versions to avoid pytree conflicts
torch>=2.1.0,<2.4.0
torchvision>=0.16.0,<0.19.0
torchaudio>=2.1.0,<2.4.0
# Transformers - compatible with torch 2.1+
transformers>=4.35.0,<5.0.0
sentence-transformers>=2.2.2,<3.0.0
numpy
scipy
scikit-learn
pandas
requests
beautifulsoup4
arxiv
neurokit2
brian2
mediapipe
opencv-python-headless
# pyaudio  # Optional: Requires system audio libraries (PortAudio)
#         # Install system dependencies first:
#         # macOS: brew install portaudio
#         # Ubuntu/Debian: sudo apt-get install portaudio19-dev
#         # Then: pip install pyaudio
websockets
pyyaml
python-multipart
python-jose[cryptography]
passlib[bcrypt]
# Fix pyasn1 version conflict - use versions compatible with python-jose
pyasn1>=0.5.0
pyasn1-modules>=0.3.0
# Quantum Computing Dependencies
dimod
# dwave-networkx  # Optional: specific version issues on some OS
# dwave-systems   # Optional: specific version issues on some OS
# dwave-ocean-sdk # Alternative if above fail
pennylane
qiskit
qiskit-algorithms
qiskit-optimization
qiskit-aer
networkx
# Multi-Agent System - Swarms Framework for Agentic LLM Operations
# Reference: https://github.com/kyegomez/swarms.git
swarms>=2.0.0
# Database & Storage
# TigerDB (TimescaleDB) - PRIMARY DATABASE (Required)
# This is the main database for all application data
psycopg2-binary

# Vector Storage
# ChromaDB - PRIMARY vector storage (Default, automatically used)
# ChromaDB is used by default for HDAM vector storage - no configuration needed
chromadb>=0.4.0

# Supabase - ALTERNATIVE vector storage (Optional)
# Only used if SUPABASE_URL and SUPABASE_KEY are configured
# If not configured, HDAM uses ChromaDB-only mode (default)
supabase
PyPDF2
python-docx
# Lemon AI (optional - can be installed separately)

# Swarms Tools - Extended agent capabilities
# Install from: https://github.com/The-Swarm-Corporation/swarms-tools.git
swarms-tools>=0.3.0

# SwarmDB - Enhanced database functionality
# Install from: https://github.com/The-Swarm-Corporation/SwarmDB.git
# Note: May need to install from source
# git+https://github.com/The-Swarm-Corporation/SwarmDB.git

# Additional dependencies for swarms-tools
httpx>=0.24.0
orjson>=3.9.0

# Note: To install SwarmDB, clone and install from source:
# git clone https://github.com/The-Swarm-Corporation/SwarmDB.git
# cd SwarmDB
# pip install -e .

# Swarm Corporation Components
# Note: Many of these packages may need to be installed from source
# If installation fails, install from GitHub:
# pip install git+https://github.com/The-Swarm-Corporation/<component>.git

# Security
swarm-shield>=0.0.1

# Document Processing
# doc-master only has versions 0.0.1, 0.0.2 available - install from source if needed
doc-master>=0.0.1
# omniparse, agentparse - install from source if not available on PyPI
# pip install git+https://github.com/The-Swarm-Corporation/omniparse.git
# pip install git+https://github.com/The-Swarm-Corporation/agentparse.git

# Research
# research-paper-hive, advanced-research - install from source if not available
# pip install git+https://github.com/The-Swarm-Corporation/research-paper-hive.git
# pip install git+https://github.com/The-Swarm-Corporation/advanced-research.git
exa-py>=1.0.0  # For AdvancedResearch web search

# RAG & Memory
# agent-rag-protocol, multi-agent-rag-template - install from source if not available
# pip install git+https://github.com/The-Swarm-Corporation/agent-rag-protocol.git
# pip install git+https://github.com/The-Swarm-Corporation/multi-agent-rag-template.git
llama-index>=0.10.0
llama-index-core>=0.10.0
chromadb>=0.4.0
# swarms-memory - install from source if not available
# pip install git+https://github.com/The-Swarm-Corporation/swarms-memory.git

# Workflow
# zero - install from source if not available
# pip install git+https://github.com/The-Swarm-Corporation/zero.git

# Utilities
# swarms-utils - install from source if not available
# pip install git+https://github.com/The-Swarm-Corporation/swarms-utils.git

# Additional dependencies
pyyaml>=6.0
